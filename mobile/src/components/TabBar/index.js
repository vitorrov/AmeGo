import React from 'react';

import { Container } from './styles';

export default function TabBar({ state, descriptors, navigation, position, ...props }) {

  return (
    <Container { ...props }>

    </Container>
  );
}
